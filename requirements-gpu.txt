# CUDA PyTorch wheels for NVIDIA GPUs with CUDA 12.8-capable drivers.
# Install this after normal requirements so PyTorch is CUDA-enabled.
--index-url https://download.pytorch.org/whl/cu128
torch==2.10.0+cu128
torchaudio==2.10.0+cu128
torchvision==0.25.0+cu128
fsspec==2025.3.0
